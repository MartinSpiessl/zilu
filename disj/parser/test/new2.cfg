names= x y
precondition= (x*x < y*y) && (y >= 0)
loopcondition=x < y
#loop=if (x>0) x++; else x--; x=-1 * x;
branchcondition=x>0
branch=x++;x=-1*x;
branchcondition=
branch=x--;x=-1*x;
postcondition=(x == y) || (x == y + 1)
learners=linear
