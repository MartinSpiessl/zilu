names=x y a
beforeloop=
beforeloopinit=
symbolic=flag
precondition=x==1 && y==1
loopcondition=
loop=if (flag){a=x+y; x++;} else {a==x+y+a;y++;} if(a%2==1) y++; else x++;
postcondition=(flag && a%2==0) || (!flag && a%2==1)
afterloop=
learners= linear poly conj
