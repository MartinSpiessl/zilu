names=a b x y u v
precondition=(x==a) && (y==b) && (u==b) && (v==0)
beforeloop=
beforeloopinit=
loopcondition=x!=y
loop= while (x>y) { x = x - y; v = u + v; } while (x<y) { y = y - x; u = u + v; }
postcondition=x*y + y*v - a*b = 0
learners=poly
