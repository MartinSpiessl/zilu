format_version: '2.0'

input_files: '$(file)'

properties:
$(reach)  - property_file: ../properties/no-overflow.prp
    expected_verdict: $(overflow_verdict)

options:
  language: C
  data_model: ILP32
