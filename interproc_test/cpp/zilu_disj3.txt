var	x:int, y:int, z:int;

begin
	if ( y>0 or x>0 or z>0) then
		while (brandom) do
			if(x>0) then
				x=x+1;
			else
				if (y>0) then
					y=y+1;
				else
					z=z+1;
				endif;
			endif;
		done;
	//x>0 or y>0 or z>0

	endif;
end
